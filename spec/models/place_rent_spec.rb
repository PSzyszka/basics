require 'rails_helper'

RSpec.describe PlaceRent do
  # it "is invalid without valid attributes" do
  #   expect(PlaceRent.new).to_not be_valid
  # end

  # it "is invalid without start_date" do
  #   place_rent = PlaceRent.create(start_date: nil, end_date:, parking:, car:)
  #   expect(place_rent).to be_invalid
  # end

  # it "is invalid without end_date" do
  #   place_rent = PlaceRent.create(start_date:, end_date: nil, parking:, car:)
  #   expect(place_rent).to be_invalid
  # end

  # it "is invalid without parking" do
  #   place_rent = PlaceRent.create(start_date:, end_date:, parking: nil, car:)
  #   expect(place_rent).to be_invalid
  # end

  # it "is invalid without car" do
  #   place_rent = PlaceRent.create(start_date:, end_date:, parking:, car: nil)
  #   expect(place_rent).to be_invalid
  # end
end
