require 'rails_helper'

RSpec.describe Car do
  # it "is invalid without valid attributes" do
  #   expect(Address.new).to_not be_valid
  # end

  # it "is invalid without start_date" do
  #   address = Car.create(registration_number: nil, model:, owner:)
  #   expect(address).to be_invalid
  # end

  # it "is invalid without start_date" do
  #   address = Car.create(registration_number:, model: nil, owner:)
  #   expect(address).to be_invalid
  # end

  # it "is invalid without start_date" do
  #   address = Car.create(registration_number:, model:, owner: nil)
  #   expect(address).to be_invalid
  # end
end
