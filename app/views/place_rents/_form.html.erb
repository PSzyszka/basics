<%= form_for @place_rent, url: parking_place_rents_path(@parking_id), local: true do |form| %>

  <% if @place_rent.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@place_rent.errors.count, "error") %> prohibited
        this place_rent from being saved:
      </h2>
      <ul>
        <% @place_rent.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.collection_select(:car_id, Car.all, :id, :model) %><br>
  <%= form.collection_select(:parking_id, Parking.all, :id, :id) %>

  <p>
    <%= form.label :starts_at %><br>
    <%= form.text_field :starts_at %>
  </p>

  <p>
    <%= form.label :ends_at %><br>
    <%= form.text_area :ends_at %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<%= link_to 'Back', parkings_path %>
